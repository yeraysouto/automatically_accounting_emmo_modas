# Documentación (LaTeX)

Documento principal:

- `docs/emma_system.tex`

## Compilar

Recomendado con `latexmk`:

```bash
cd docs
latexmk -pdf emma_system.tex
```

Alternativa (si no tienes latexmk):

```bash
cd docs
pdflatex emma_system.tex
pdflatex emma_system.tex
```
